```python
import numpy as np

def select_next_node_v2(current_node: int, destination_node: int, unvisited_nodes: set, distance_matrix: np.ndarray) -> int:
    """Select the next node to visit from the unvisited nodes using a look-ahead heuristic."""
    # Assign a score to each unvisited node based on the minimum distance to the remaining nodes
    scores = {}
    for node in unvisited_nodes:
        # Calculate the direct distance from the current node to the potential next node
        direct_distance = distance_matrix[current_node][node]
        # Look-ahead: Calculate the minimum distance from the potential next node to all other unvisited nodes
        look_ahead_distance = min(distance_matrix[node][i] for i in unvisited_nodes if i != node)
        # Combine the direct distance and the look-ahead distance to calculate a score
        scores[node] = direct_distance + look_ahead_distance

    # Select the node with the lowest combined score (shortest total distance)
    next_node = min(scores, key=scores.get)
    return next_node
```
